<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <TextView
        android:id="@+id/follow_tag_tip"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerInParent="true"
        android:layout_marginBottom="32dp"
        android:layout_marginTop="32dp"
        android:gravity="center"
        android:paddingLeft="32dp"
        android:paddingRight="32dp"
        android:text="@string/follow_tag_tip" />

    <ImageView
        android:id="@+id/image_logo_gray"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:layout_above="@+id/follow_tag_tip"
        android:layout_centerHorizontal="true"
        android:src="@mipmap/logo_gray" />

    <Button
        android:id="@+id/add_tag_btn"
        android:layout_width="wrap_content"
        android:layout_height="36dp"
        android:layout_below="@+id/follow_tag_tip"
        android:layout_centerHorizontal="true"
        android:background="@drawable/add_tag_btn_selector"
        android:text="@string/follow_tag_btn"
        android:textColor="@android:color/white" />
</RelativeLayout>